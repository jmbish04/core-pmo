{
  "name": "gas-project",
  "version": "1.0.0",
  "description": "Google Apps Script project managed with clasp",
  "scripts": {
    "lint": "eslint src/**/*.ts",
    "validate": "node scripts/gas-addon/validate-config.js",
    "build": "node scripts/gas-addon/build.js",
    "push": "npm run build && npx clasp push -f",
    "deploy": "node scripts/gas-addon/deploy-prod.js"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
